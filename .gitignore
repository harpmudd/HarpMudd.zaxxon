# ── Quartus build intermediates (regenerated by compilation) ──────────────────
src/fpga/db/
src/fpga/incremental_db/
src/fpga/output_files/
src/fpga/ap_core_assignment_defaults.qdf
src/fpga/c5_pin_model_dump.txt
*.qws
*.rpt
*.summary
*.smsg
*.jdi

# ── Build log ─────────────────────────────────────────────────────────────────
quartus_build.log
compile_log.txt

# ── Claude Code local tooling ─────────────────────────────────────────────────
.claude/

# Note: dist/ IS committed on purpose — it holds the built bitstream + ROM image,
# so reverting to a baseline gives a deployable core without recompiling.
